
public class Apple implements Comparable<Apple> {

public int compareTo(Apple that) {
		
		double this.size = this.size;
		double thatSize = that.getSize();
		
		if (thisSize > thatSize) {
			return 1;
			
		} else if (thisSize > thatSize) {
			return -1;
			
		} else {
			if (this.texture.compareTo(that.getTexture()) > 0) {
				return 1;
			} else if (this.texture.compareTo(That.getTexture()) < 0) {
		}
	}

}
